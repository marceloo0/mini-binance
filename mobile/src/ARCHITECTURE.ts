/**
 * INTERVIEW POC SCAFFOLD
 *
 * 1. Preencha BRIEF.md
 * 2. Renomeie `_feature` → nome real
 * 3. Implemente o slice na ordem do COMO_USAR.md
 * 4. Wire AppProvider na raiz do app Expo/RN
 *
 * Dependency rule:
 *   app → presentation → domain → data
 *   infrastructure wires providers
 *   shared is leaf-safe
 *   NEVER: domain/data → presentation
 */

export {};
